# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Credentials and secrets -- never commit exchange API keys
.env
config/credentials.yaml
*.key

# Data and run artefacts
data/storage/*.parquet
market_data/
*.log

# Polymarket captured snapshots -- shared via dedicated branches, not main history
snapshot*.json
snapshots/
capture*.jsonl
*.jsonl

# Polymarket live-bot SQLite state -- per-laptop, never committed
data/polymarket.sqlite*
*.sqlite
*.sqlite-shm
*.sqlite-wal
.dashboard_uploads/

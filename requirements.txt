# Core (required)
numpy>=1.24
pandas>=2.0

# Optional -- the toolkit degrades gracefully when these are absent:
statsmodels>=0.14    # cointegration testing + Benjamini-Hochberg FDR
scikit-learn>=1.3    # Ledoit-Wolf covariance and clustering (portfolio layer)
pyyaml>=6.0          # YAML config loading in config/settings.py
ccxt>=4.0            # live exchange data in data/ingestion.py
pyarrow>=14.0        # Parquet candle files in data/ingestion.load_ohlcv
ib_insync>=0.9.86    # Interactive Brokers data + execution in data/ibkr.py

# Development
pytest>=7.0
